# CLAUDE.md — Claude Code entry point

All standing instructions live in `AGENTS.md`. Read it first, every session.
Skills are in `skills/`. Run `/start` at the beginning of every session.

@AGENTS.md
